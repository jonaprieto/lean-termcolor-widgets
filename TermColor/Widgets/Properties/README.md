# TermColor.Widgets.Properties

`TermColor.Widgets.Properties` is a build-only library of machine-checked rendering examples. It covers
completion and overflow behavior, explicit progress widths, optional percentages, style
preservation, spinner frame wrapping, custom frames, and empty-frame safety.

```sh
lake build TermColor.Widgets.Properties
```
